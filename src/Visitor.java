public interface Visitor {
  //visitPartition(Partition partition);
  //visitSpace(Space space);
}


//Class FindAreaById implements Visitor

//Class GetDoorsGivingAccess implements Visitor

//Clases de metodos de classes partition y space

//en clase area meter un acceptVisitor() y override en space y partition